define([
    'Model'
], function(Model) {
    'use strict';
    class model extends Model {
        constructor(config) {
            super(config);
        }

        _loadData() {

        }

        _refresh() {

        }
    }

    return model;
});