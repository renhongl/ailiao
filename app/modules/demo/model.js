
define(['Model'], function(Model) {
    'use strict';
    class model extends Model {
        constructor(obj, config) {
            super(obj, config);
        }

        _loadData() {
           
        }

        _refresh() {

        }
    }

    return model;
});