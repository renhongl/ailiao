'use strict';

import Model from '/core/Model'

export default class model extends Model{
    constructor(config){
        super(config);
    }

    _loadData(){
        
    }

    _refresh(){
         
    }
}
